#include <stdio.h>
#include <stdbool.h>
int main(){
    int arr[50],num,i=0;
    while(true){ 
        scanf("%d",&num);
        if(num<=150){
            if(num%5==0){
                arr[i]=num;
                i+=1;
            }
        }
        else
        break;
    }
    for(int j=0;j<i;j++){
        printf("%d ", arr[j]);
    }
    return 0;
}
