//Arranging string in lexicographical order
#include <stdio.h>
#include <string.h>
int main()
{
    int n;
    scanf("%d",&n);
    char s[n][50],temp[50];
    for(int i=0;i<=n;i++)
    {
        fgets(s[i],50,stdin);
    }
    for(int i=0;i<=n;i++)
    {
        for(int j=i+1;j<=n;j++)
        {
            if(strcmp(s[i],s[j])>0)
            {
                strcpy(temp,s[i]);
                strcpy(s[i],s[j]);
                strcpy(s[j],temp);
            }
        }
    }
    for (int i=0;i<=n;i++) {
      fputs(s[i], stdout);
   }
   return 0;
}
